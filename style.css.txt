body {
  font-family: sans-serif;
  text-align: center;
  background: #f0f0f0;
}
#game-board {
  display: grid;
  grid-template-columns: repeat(5, 60px);
  gap: 10px;
  justify-content: center;
  margin-top: 20px;
}
.cell {
  width: 60px;
  height: 60px;
  background: white;
  border: 2px solid #333;
  font-size: 24px;
  line-height: 60px;
  cursor: pointer;
  user-select: none;
}
.cell.selected {
  background: yellow;
}
